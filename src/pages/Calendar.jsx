import React from "react";
import BottomBar from "../components/common/BottomBar";

const Calendar = () => {
    return (
      <div>
        Calendar
        <BottomBar />
      </div>
    );
  };
  export default Calendar;