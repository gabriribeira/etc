import React from "react";
import BottomBar from "../components/common/BottomBar";

const Expense = () => {
    return (
      <div>
        Expense
        <BottomBar />
      </div>
    );
  };
  export default Expense;