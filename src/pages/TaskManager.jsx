import React from "react";
import BottomBar from "../components/common/BottomBar";

const TaskManager = () => {
    return (
      <div>
        Task Manager
        <BottomBar />
      </div>
    );
  };
  export default TaskManager;